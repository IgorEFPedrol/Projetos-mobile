import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'prj-academia',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
