import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-vai-dar-namoro',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
